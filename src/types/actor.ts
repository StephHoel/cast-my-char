export type ActorProps = {
  id: string
  name: string
  nationality: string[]
  eyeColor: string
  hairColor: string[]
  ageRange: string[]
  gender: string[]
  tags: string[]
  photoUrl: {
    big: string
    short: string
  }
}
